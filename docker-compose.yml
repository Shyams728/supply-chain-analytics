version: '3.8'

services:
  app:
    build: .
    image: supply-chain-analytics:latest
    container_name: supply_chain_app
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    # If we add a database later:
    # depends_on:
    #   - db

  # Placeholder for future database integration
  # db:
  #   image: postgres:15
  #   environment:
  #     POSTGRES_USER: user
  #     POSTGRES_PASSWORD: password
  #     POSTGRES_DB: supply_chain
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

# volumes:
#   postgres_data:
